<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Read service name -->
    <springProperty name="appName" source="spring.application.name"/>
    <property name="LOG_FILE" value="logs/${appName:-application}.json"/>

    <!-- ===== Console Appender ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${appName:-}, trace=%X{traceId:-}, span=%X{spanId:-}] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- ===== File Appender ===== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- IMPORTANT: absolute path -->
        <file>${LOG_FILE}</file>

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "timestamp": "%date{ISO8601}",
                        "level": "%level",
                        "service": "${appName}",
                        "traceId": "%X{traceId:-}",
                        "spanId": "%X{spanId:-}",
                        "thread": "%thread",
                        "logger": "%logger",
                        "message": "%message"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- ABSOLUTE PATH -->
            <fileNamePattern>logs/${appName}-%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

    </appender>

    <!-- ===== Root Logger ===== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
